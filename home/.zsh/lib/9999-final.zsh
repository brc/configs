# syntax highlighting
#
#   XXX Per the documentation, this code *always* has to run last if you want
#       it to work right.  Hence, not being loaded inline with other bundles.
#
name=zsh-syntax-highlighting
source ~/.zsh/bundles-available/zsh-users/${name}/${name}.plugin.zsh

