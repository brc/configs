# vim:set ft=conf:

[global_config]
focus = system
title_receive_bg_color  = colour234
title_receive_fg_color  = colour249
title_transmit_bg_color = colour234
title_transmit_fg_color = colour249
title_inactive_bg_color = colour234
title_inactive_fg_color = colour249


[keybindings]
    # Got these strings with:
    #
    #   $ man terminator_config \
    #         |grep -A140 '^keybindings' \
    #         |grep -E '^[[:space:]]+[a-z_]+$' \
    #         |sed 's/$/ =/' \
    #         |sort
    #
    close_term =
    close_window =
    cycle_next =
    cycle_prev =
    edit_tab_title =
    edit_terminal_title =
    edit_window_title =
    full_screen =
    go_down =
    go_left =
    go_prev =
    go_right =
    group_all =
    group_tab =
    hide_window =
    move_tab_left =
    move_tab_right =
    new_tab =
    new_terminator =
    new_window =
    next_tab =
    prev_tab =
    reset_clear =
    resize_down =
    resize_left =
    resize_right =
    resize_up =
    rotate_ccw =
    rotate_cw =
    scaled_zoom =
    split_horiz =
    split_vert =
    toggle_scrollbar =
    toggle_zoom =
    ungroup_all =
    ungroup_tab =
    #zoom_in =
    zoom_normal =
    #zoom_out =

[profiles]
[[default]]
    alternate_screen_scroll = True
    audible_bell = False
    background_color = "#192837"
    #background_darkness = 0.8
    #background_type = "transparent"
    backspace_binding = "control-h"
    copy_on_selection = True
    cursor_color = "magenta"
    #cursor_shape = "underline"
    custom_command = "tmux -2 -u"
    foreground_color = "#d3d4d4"
    scroll_on_output = False
    scrollback_lines = 500
    use_custom_command = True
    use_theme_colors = False
    visible_bell = True
    word_chars = '-A-Za-z0-9,./?%&#:_~'

#    background_color = "#FFFFFF"
#    foreground_color = "#d3d4d4"
